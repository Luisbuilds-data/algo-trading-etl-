name: 'trading_warehouse'
version: '1.0.0'
config-version: 2

profile: 'trading_warehouse'

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  trading_warehouse:
    staging:
      +materialized: view
      +schema: staging
    dimensions:
      +materialized: table
      +schema: marts
    facts:
      +materialized: table
      +schema: marts
    marts:
      +materialized: table
      +schema: marts
